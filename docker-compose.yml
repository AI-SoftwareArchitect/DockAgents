version: "3.8"

services:
  chromadb:
    image: chromadb/chroma:latest
    container_name: dockagents-chromadb
    ports:
      - "8001:8000"
    volumes:
      - chroma_data:/chroma/chroma
    restart: unless-stopped

volumes:
  chroma_data:
